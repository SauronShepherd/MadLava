# Getting started with I01

Build with Java 11 and the `generic` profile, attach the produced JAR at JVM startup, reproduce one workload, inspect the JSONL file with the offline viewer, and remove the agent after the investigation unless it is intentionally retained. I01 output is bounded process-local evidence, not a causal diagnosis.
