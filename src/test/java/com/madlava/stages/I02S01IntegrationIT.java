package com.madlava.stages;import static org.junit.jupiter.api.Assertions.*;import com.madlava.config.*;import java.util.Map;import org.junit.jupiter.api.Test;public class I02S01IntegrationIT{@Test void configurationPrecedenceAndValidation(){var r=new ConfigurationResolver(ConfigurationMetadata.baseline());var c=r.resolve(Map.of("enabled",false),Map.of("enabled","true"),"explicit");assertEquals(true,c.values().get("enabled"));assertEquals(64,c.hash().length());}}
