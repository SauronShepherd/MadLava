package com.madlava.stages;import static org.junit.jupiter.api.Assertions.*;import com.madlava.core.*;import java.time.Clock;import org.junit.jupiter.api.Test;public class I02S02IntegrationIT{@Test void capabilitiesAndFailureIsolation(){assertFalse(RuntimeCapabilities.detect().values().isEmpty());var b=new FeatureCircuitBreaker(1,1000,Clock.systemUTC());b.failure();assertFalse(b.allow());}}
